services:
  sql:
    image: mysql:8
    container_name: love
    restart: always
    volumes:
      - mysql_data:/var/lib/mysql
    environment:
      - MYSQL_ROOT_PASSWORD=root
    ports:
      - "3307:3306"
volumes:
  mysql_data: