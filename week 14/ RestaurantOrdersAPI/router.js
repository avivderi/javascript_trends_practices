import express from 'express'

const router = express.Router()

router.get('/', (err, req, res, next) => {
    try {
        const query = {status, customer, table,}
    } catch (error) {
        
    }
})

router.get('/:id', (err, req, res, next) => {
    
})

export default router