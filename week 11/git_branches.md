1.
בראנץ' משמעותו ענף... והוא עוזר לי להמשיך לפתח ואםילו לעשות קונ=מיט בלי לדרוס את מה שכרגע עובד.... וכך אני יוכל להשלים את הפיצר החדש בלי שהקובץ הראשי יקרוס

2.
הענף main  הוא הענף הראשי הוא הגזע שממנו יוצאים לכל הבראנצים האחרים

3.
לא הוא לא מייצר עותק... אלא מביא גישה לכומיט בענף חדש שנותן אופציה לעריכה

4.
git checkout -b 'loggin-error'

5.
git checkout loggin-error

6.
git checkout main

7.
הכוונה ב - HEAD זה המיקום שאנחנו עובדים עליון עכשיו... והוא הראשי כרגע 

8.
אז ה - main  יהיה ה HEAD

9.
לא נכון.... הוא מצביע על הבראנץ הנוכחי

10.
git checkout main

11.
git checkout main
git merge feature/signup-form

12.
git branch -d feature/signup-form

13.
מניעה של עומס בבראנצים ובלאגן מיותר

14.
git checkout -b 'feature/notifications'
git commit -m "Add notification bell icon"
git branch
git switch main

15.
git checkout main
git merge feature/notifications
git branch -d feature/notifications